# <--------------> load data script params
local_dir: "./Data"
dataset: "ayuseless/streetview-image-dataset"
split: 0.9
seed: 42
# <-------------->

# hyperparameters
batch_size: 64
learning_rate: 0.0001
epochs: 10
regularize: "l1" # l2 or l1
weight_decay: 0.001