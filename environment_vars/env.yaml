# call conda env create -f "./environment_vars/env.yaml"
name: ViT
channels:
  - default
dependencies:
  - python=3.10
  - matplotlib
  - tqdm
  - pip
  - pip:
    - torch
    - torchvision
    - numpy
    - pandas
    - huggingface_hub

